NEXT_PUBLIC_MAPBOX_TOKEN=
NEXT_PUBLIC_ORS_API_KEY=

# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel full-stack deployment:
# Leave NEXT_PUBLIC_API_URL unset so the frontend calls the same-origin /api routes.
